#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <unistd.h>
#include <string.h>
#include <stdio.h>

int print_binary(unsigned int n);
void print_num(int n);
int _printf(const char *format, ...);

#endif
